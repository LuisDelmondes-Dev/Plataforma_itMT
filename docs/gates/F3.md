# Phase Gate F3 — IA avançada, campo e GIS

Data: 15/08/2026  
Decisão: `PHASE_NOT_APPROVED_EXTERNAL`

## Entrega técnica comprovada

- gateway Anthropic/OpenAI, cascata, telemetria e fallback léxico;
- contratos runtime A01/A04/A05/A06, validação de entrada/saída, allowlists, timeout, retry, fallback e registro por etapa;
- PWA instalável, app shell offline e outbox IndexedDB cifrada com AES-GCM;
- formulários de campo versionados, blobs persistidos e sincronização idempotente;
- storage local/S3 com namespace tenant, SHA-256, criptografia, URLs assinadas e quarentena antivírus de documentos;
- Cesium/3D Tiles com metadados, bounds, CRS, hash e fallback acessível;
- projeções e cenários determinísticos, vetos de consentimento/licença/publicação e contrato unificado de ativos;
- regressão integral `131/131`, build web e fluxo real no navegador sem erros de console.

## Pendências que não podem ser fabricadas no repositório

- campanha domiciliar e campanha de campo reais, com base legal, consentimento e equipe;
- VANT, 360° e 8K: equipamentos, operadores, RT e autorizações DECEA/ANAC;
- dez pacotes municipais reais e 100 indicadores publicados por curadoria humana;
- homologação de object storage, antivírus e GIS no ambiente contratado;
- cobertura de todos os agentes A01–A15 e pipeline real de derivados de mídia, caso mantidos no escopo contratado.

```text
SOFTWARE_GATE_IMPLEMENTED_SCOPE: PASS
REGRESSION_GATE: PASS_129_OF_129
FIELD_AND_REAL_ASSET_GATE: BLOCKED_EXTERNAL
FINAL_DECISION: PHASE_NOT_APPROVED_EXTERNAL
```

## Adendo de regressão — 22/08/2026

O bloco acima é o registro de 15/08/2026 e fica preservado como está. Ele diverge
do texto desta mesma página ("regressão integral `131/131`"), que já refletia a
contagem posterior: `129` foi a marca de `EV-20260815-034` e `131` a de
`EV-20260819-037` em diante.

Reexecução local em 22/08/2026, banco descartável criado do zero — `45` migrações,
`131/131` testes e cadeia de auditoria íntegra com `131` eventos:

```text
REGRESSION_GATE_20260822: PASS_131_OF_131
FINAL_DECISION: PHASE_NOT_APPROVED_EXTERNAL (inalterada)
```

A decisão da fase não muda: o bloqueio de F3 é de campo e ativos reais, não de
regressão.
